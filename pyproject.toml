[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mlb_analysis"
version = "0.1.0"
description = "Kalshi MLB Trading Strategy Research & Implementation"
dependencies = []

[tool.setuptools]
packages = ["data", "models", "utils"]